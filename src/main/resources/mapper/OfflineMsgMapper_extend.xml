<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chen.mapper.extend.OfflineMsgMapperExtend">
	
	<!-- MsgStatus_Read = 4; //对方已读 -->
	<select id="selectOfflineListByReceiver_id" parameterType="java.lang.String"
		resultMap="BaseResultMap">
		select
		<include refid="Base_Column_List" />
		from tb_offline_msg
		where
		receiver_id = #{receiver_id} 
		and
		msg_status != 4
		
	</select>

	<update id="deleteOfflineMsg"
		parameterType="com.chen.packet.ReceiptPacket">
		update tb_offline_msg 
		set
		 msg_status = 4
		
		where msg_id = #{msg_id}
		and sender_id = #{sender_id} 
		and receiver_id = #{receiver_id}
	</update>
	
</mapper>